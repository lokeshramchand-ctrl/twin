<!-- getUser() returns JSON format.
createUser() validates email before storing.
User data is stored in PostgreSQL.
User cache is disabled by default. -->
getUser() returns XML format.
createUser() validates phone number before storing.
User data is stored in MongoDB.
User cache is enabled by default.